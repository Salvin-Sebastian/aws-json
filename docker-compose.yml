version: '3.8'

services:
  db:
    image: postgres:15
    container_name: aws-postgres-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: salvin123
      POSTGRES_DB: awsdb
    ports:
      - "5432:5432"
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
